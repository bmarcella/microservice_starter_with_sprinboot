# package manager for kubernates 
# pip is package manager for python
# Hal and chart 
Helm create {nom_du_chart}
## THOSE tag is to define service model
{{- define "common.service" -}}
 ## SERVICE MODEL
{{- end -}}   

## THOSE tag is to define deployment model
{{- define "common.deployment" -}}
 ## deployment MODEL
{{- end -}}  

# those tag is for IF Predicate 
{{- if   .Values.profile_enabled }}
    
{{- end }}  

helm dependency build

helm dependencies build

helm template .